#include "main.h"

/**
 * _isdigit - Tes for numbers
 * @c: character in check
 *
 * Return:  diffent of 1 if is number and 0 otherwise
 **/

int _isdigit(int c)
{

	if
		(c >= '0' && c <= '9')
		return (1);
	else
		return (0);
}
